# === FILE: site_scout_project/.isort.cfg ===
[settings]
# Use Black-compatible import formatting
profile = "black"
# Match Black's default line length
line_length = 88
# Explicitly sort imports in the source package
src_paths = ["site_scout"]
# Skip virtual environment and test directories
skip = venv, tests
